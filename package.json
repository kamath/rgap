{
  "name": "rgap",
  "private": true,
  "packageManager": "pnpm@11.10.0",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "test": "pnpm -r --if-present test",
    "scratch": "pnpm --filter @rgap/examples start",
    "docs:dev": "pnpm --filter @rgap/docs dev",
    "docs:build": "pnpm --filter @rgap/docs build"
  }
}
